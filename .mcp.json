{
  "mcpServers": {
    "salesforce-dx": {
      "command": "npx",
      "args": [
        "-y",
        "@salesforce/mcp",
        "--orgs",
        "vscodeOrg",
        "--toolsets",
        "metadata,data,orgs,users",
        "--allow-non-ga-tools"
      ]
    }
  }
}
